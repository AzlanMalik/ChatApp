version: 2.1

# Define the jobs we want to run for this project
jobs:
  build:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: echo "cloned the repo"
      - run: ls
      - run: cp .env.example .env
      - run: cat .env

# Orchestrate our job run sequence
workflows:
  testing-config:
    jobs:
      - build
